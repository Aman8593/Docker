import React from 'react';
import './App.css'; // Assuming you have some CSS styles in App.css

function App() {
  return (
    <div className="App">
      <header className="App-header">
        <p>
        Containerization is a lightweight alternative to full machine virtualization that involves encapsulating an application in a container with its own operating environment. This allows the application to run on any computing environment, whether physical or virtual, without changes.
        </p>
        <a
          className="App-link"
          href="https://reactjs.org"
          target="_blank"
          rel="noopener noreferrer"
        >
          Learn React
        </a>
      </header>
    </div>
  );
}

export default App;
